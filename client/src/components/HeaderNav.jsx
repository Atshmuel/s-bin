function HeaderNav() {
    return (
        <div className="bg-red-400 px-8 py-3 flex justify-between">
            <p>Shmuel atar</p>
            <div>
                <span>theme toggle  + (icon)</span>
                <span>Manage settings + (icon)</span>
                <span>logout + (icon)</span>
            </div>
        </div>
    )
}

export default HeaderNav
