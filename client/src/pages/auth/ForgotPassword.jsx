function ForgotPassword() {
    //should contain a form with input of email, after submittion show a message to check email and then change the ui to otp inputs after that redirect to reset password page
    return (
        <div>

        </div>
    )
}

export default ForgotPassword
