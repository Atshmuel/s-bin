version: "3.9"

services:
  server:
    image: shmuela/sbin-server:latest
    container_name: sbin-server
    restart: always
    env_file:
      - ./server/.env

  client:
    image: shmuela/sbin-client:latest
    container_name: sbin-client
    restart: always
    depends_on:
      - server
    ports:
      - "80:80"
    volumes:
      - ./client/nginx.conf:/etc/nginx/conf.d/default.conf:ro